{% extends 'base.html.twig' %}

{% block title %}MotCle{% endblock %}

{% block body %}
    <h1>MotCle</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Mot_cle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mot_cle in mot_cles %}
            <tr>
                <td>{{ mot_cle.id }}</td>
                <td>{{ mot_cle.motCle }}</td>
                <td>
                    <a href="{{ path('mot_cle_show', {'id': mot_cle.id}) }}">Afficher</a>
                    <a href="{{ path('mot_cle_edit', {'id': mot_cle.id}) }}">Editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Liste vide</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('mot_cle_new') }}">Créer/a>
{% endblock %}
