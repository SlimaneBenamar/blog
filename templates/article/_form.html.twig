{{ form_start(form) }}
{#
{{ form_widget(form) }}
#}

    <div class="row">
        <div class="col-md-4">{{ form_row(form.titre) }}</div>
        <div class="col-md-4">{{ form_row(form.contenu) }}</div>
        <div class="col-md-4">{{ form_row(form.date_creation) }}</div>
        <div class="col-md-4">{{ form_row(form.date_modification) }}</div>
    </div>
    <div class="row">
        <div class="col-md-2">{{ form_row(form.id_categories) }}</div>
        <div class="col-md-2">{{ form_row(form.id_motcle) }}</div>
        <div class="col-md-2">{{ form_row(form.imageFile) }}</div>
    </div>

{{ form_rest(form) }}
<button class="btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
